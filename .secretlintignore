# .secretlintignore — `.gitignore` syntax.
# `**/.git/**`, `**/node_modules/**`, and the secretlint config are ignored by default.
# .gitignore is respected automatically since v13 (so dist/, coverage/, node_modules/ are skipped).
#
# Add entries below ONLY when a real false positive shows up — empty start is correct.
# Do NOT broadly ignore tests/fixtures/ or examples/ so real secrets that land there are caught.
